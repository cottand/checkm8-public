#include <stdlib.h>
#include "Emulator.h"
#include "Loader.h"
#include "data_proc.h"

#include <stdio.h>

int main(int argc, char **argv)
{
  // char *path = argv[1];
  // Emulator em;
  // emulator_init(&em);
  
  // load(path, em.mem);
}
